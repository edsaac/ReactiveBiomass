/*--------------------------------*- C++ -*----------------------------------*\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Version:  7
     \\/     M anipulation  |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       volScalarField;
    object      h;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

dimensions      [0 1 0 0 0 0 0];

internalField   uniform -10;

boundaryField
{
    front
    {
        type            empty;
    }

    back
    {
        type            empty;
    }

    right
    {
        type        fixedGradient;
        gradient    uniform 0.0;
        // type		cyclicAMI;
    }

    left
    {
  	    // type		cyclicAMI;
          type        fixedGradient;
          gradient    uniform 0.0;  // Negative means outside the patch
    }

    top
    {
        type        fixedValue;
	    value       uniform  0.1; 
        // type        codedFixedValue;
        // value       uniform 0.1;

        // name        bumpInTheMiddle;
        // code
        // #{
        //     const vectorField& Cf = patch().Cf();
        //     scalarField& field = *this;

        //     const scalar hWet = 0.1;
        //     const scalar hDry = -10.0;

        //     forAll(Cf, faceI)
        //     {
        //         scalar x = Cf[faceI][0];
        //         // field[faceI] = hWet + hDry*sqr(x - 0.5);
        //         if(x > 0.4 && x < 0.6){field[faceI] = hWet;}
        //         else{field[faceI] = hDry;}
        //     }
        // #};

    }

    bottom
    {
        type        fixedGradient;
        gradient    uniform 1.00;   // Set to 1.0 for a NO-FLOW condition
        // type fixedValue;
        // value   uniform 0.01;
    }
}

// ************************************************************************* //
