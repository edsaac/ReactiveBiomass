/*--------------------------------*- C++ -*----------------------------------*\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Version:  7
     \\/     M anipulation  |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       volScalarField;
    object      h;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

dimensions      [0 1 0 0 0 0 0];

internalField   uniform -1;

boundaryField
{
    front
    {
        type            empty;
    }

    back
    {
        type            empty;
    }

    right
    {
  	type		cyclicAMI;
    }

    left
    {
  	type		cyclicAMI;
    }

    top
    {
        type        fixedValue;
	    value       uniform  0.1; 
        // type        codedFixedValue;
        // value       uniform 0.01;

        // name        bumpInTheMiddle;
        // code
        // #{
        //     const vectorField& Cf = patch().Cf();
        //     scalarField& field = *this;

        //     const scalar hWet = 0.01;
        //     const scalar hDry = -1.0;

        //     forAll(Cf, faceI)
        //     {
        //         scalar x = Cf[faceI][0];
        //         field[faceI] = hWet + hDry*sqr(x - 0.5);
        //         // if(x > 0.4 && x < 0.6){field[faceI] = hWet * 1-4\left(x-0.5\right)^{2};}
        //         // else{field[faceI] = hWet;}
        //     }
        // #};

    }

    bottom
    {
        type        fixedGradient;
        gradient    -0.5;
        // type fixedValue;
        // value   uniform 0.01;
    }
}

// ************************************************************************* //
